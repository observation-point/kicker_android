<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTextStyle">
        <item name="android:fontFamily">@font/consola</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ConfirmButton">
        <item name="android:fontFamily">@font/consola</item>
        <item name="android:background">@drawable/button_confirm_background_selector</item>
        <item name="android:elevation">10dp</item>
        <item name="android:textSize">@dimen/text_size_20sp</item>
        <item name="android:lines">1</item>
        <item name="android:textColor">@drawable/button_confirm_text_white_selector</item>
    </style>

</resources>
